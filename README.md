pNotes is just a very simple open-source notepad written using Python with wxPython library. It is basically copy of the early versions of wEditor but with Python.

To use the source code install the wxPython and than you can just simply run it.
